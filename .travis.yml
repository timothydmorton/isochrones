sudo: false
language: generic
matrix:
    include:

        # Linux
        - os: linux
          env: PYTHON_VERSION=3.6

        # OSX
        - os: osx
          env: PYTHON_VERSION=3.6 MPLBACKEND=Qt4Agg

install:
    - source .ci/travis.sh

script:
    - nosetests -v isochrones
